import unittest

def coin_change(denominations: list[int], amount: int) -> int:
    """
    Returns the lowest of coins for a given amount if possible
    """

class CoinChangeTest(unittest.TestCase):
    pass

if __name__ == '__main__':
    unittest.main()